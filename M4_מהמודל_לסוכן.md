# M4: מהמודל לסוכן 🤖

https://technion-lbs-course.github.io/Class-Practice/

## מאיפה באנו ולאן הולכים

ב-[M3](M3_האם_המודל_שלכם_טוב.md) בניתם מודל שמקבל קלט מובנה ומחזיר מספר. הבעיה: היוזר לא מדבר ב-DataFrame. הוא כותב משפט. **M4 הוא הסוכן** — השכבה שמתרגמת בין השפה החופשית של היוזר למודל שלכם, ובחזרה.

הסוכן לא מחליף את המודל. הוא **עוטף** אותו.

> 🧭 **הרעיון המרכזי:** הסוכן לוקח מה שהיוזר אומר בשפה חופשית, מחלץ ממנו את הפרמטרים, מריץ את **המודל שלכם**, ומחזיר תשובה מנוסחת. המספר תמיד מגיע מהמודל שלכם — ה-LLM רק מתרגם קלט ופלט. בלי השלב הזה בניתם צ'אט, לא מוצר.

---

## הלולאה — ארבעה שלבים

1. היוזר כותב בשפה חופשית.
2. ה-LLM מחלץ מהטקסט את הפרמטרים שהמודל צריך (כ-JSON).
3. **המודל שלכם** מריץ `predict` על הפרמטרים.
4. מציגים ליוזר את התוצאה (טקסט פשוט, או ניסוח של ה-LLM).

שלב 3 הוא הלב. אם הסוכן שלכם נעצר בשלב 2 — חילצתם פרמטרים ולא קראתם למודל — בניתם מחלץ טקסט, לא מוצר חיזוי.

---

## תבנית 4 השדות — תתחילו מפה

ארבע שורות שמגדירות את הסוכן שלכם. הן גם **מסדרות את הפרויקט**: מי שמתקשה להגדיר מה הוא בונה, השאלה "מה היוזר נותן ומה המודל צריך" פותרת לו את זה.

- היוזר אומר: ______ (טקסט חופשי)
- הפרמטרים שאני מחלץ: ______
- הקלט שהמודל שלי צריך: ______
- מה שחוזר ליוזר: ______ (הפלט של המודל, מנוסח ביושר)

> 🟦 רגרסיה: היוזר כותב "דירה 3 חדרים בתל אביב, 80 מטר" ← מחלצים `{rooms, city, area}` ← המודל מחזיר מחיר ← "המחיר המשוער: 2.4M".
> 🟩 סיווג / סגמנטציה: היוזר מתאר מקום ← מחלצים את הפיצ'רים ← המודל מחזיר קטגוריה או סגמנט ← "המקום הזה שייך לסגמנט X".
> 🟨 זיהוי (קלט תמונה): כאן הקלט הוא **תמונה**, לא טקסט. הסוכן עוטף קריאה ל-API חיצוני (מיקום / מזג אוויר) ואת הרצת הגלאי על התמונה, ומחזיר התראה מנוסחת.

---

## פרומפט: system מול user

- ה-system: כותבים אותו **פעם אחת**. מי הסוכן, מה מותר ואסור לו, ובאיזה פורמט להחזיר (JSON עם שדות מוגדרים). זה הגבול — היוזר לא יכול לדרוס אותו.
- ה-user: **משתנה בכל פנייה**. זה הטקסט החופשי של היוזר.

החוקים שחשוב לזכור: ה-LLM הוא **stateless** (שולחים לו את כל ההקשר בכל קריאה), `temperature` נמוך כשרוצים JSON אמין, והמפתח נשמר ב-`st.secrets` ולא נכנס לעולם לקוד או לגיט.

---

## שלושת הרכיבים של M4

1. ולידציית קלט — לבדוק שמה שחולץ הגיוני (שדות לא חסרים, ערכים בטווח).
2. הנדסת prompt — system ברור עם פורמט פלט מוגדר.
3. ולידציית פלט + Fallback — אם ה-LLM החזיר משהו לא תקין, לתפוס את זה ולתת ברירת מחדל. LLM יכול להחזיר טקסט במקום JSON או להמציא — זה מובנה, ולכן ה-Fallback הוא חובה.

---

## המימוש: Groq + Llama (חינם)

מקבלים מפתח חינם ב-groq.com ומריצים מודלים פתוחים בחינם (llama / gpt-oss). שימו לב: **Groq תואם-OpenAI, לא Anthropic** — הקריאה היא `client.chat.completions.create`, לא `client.messages.create`.

```python
# pip install groq    (Groq is OpenAI-compatible, NOT Anthropic)
import json, pandas as pd, streamlit as st
from groq import Groq

client = Groq(api_key=st.secrets["GROQ_API_KEY"])   # never hard-code the key, never commit it

SYSTEM = (
    "You extract parameters from the user's free text. "
    "Return ONLY JSON with the keys: area, rooms, city. No extra text."
)

def extract_params(user_text: str) -> dict:
    resp = client.chat.completions.create(
        model="llama-3.3-70b-versatile",   # or openai/gpt-oss-20b — pick from Groq's list
        temperature=0,                     # low temperature for reliable JSON
        messages=[
            {"role": "system", "content": SYSTEM},
            {"role": "user",   "content": user_text},
        ],
    )
    try:
        return json.loads(resp.choices[0].message.content)   # output validation
    except json.JSONDecodeError:
        return {"area": 80, "rooms": 3, "city": "תל אביב"}    # Fallback

# --- the full loop ---
user_text = st.text_input("מה תרצו לחזות?")
if user_text:
    params = extract_params(user_text)              # steps 1-2: LLM extracts params
    y = pipe.predict(pd.DataFrame([params]))[0]     # step 3: YOUR model predicts
    st.success(f"תוצאה: {y:.1f}")                    # step 4: present the model's number
```

---

## החצי השני: משכבת הקלט לשכבת הפלט המנחה

עד כאן הסוכן לקח טקסט חופשי והחזיר את **המספר** של המודל. אבל מספר הוא עדיין לא התשובה — הוא הצעד שלפניה. השכבה השלישית של הקורס (תיאורי, חיזוי, **מנחה**) היא ההמלצה: מה היוזר צריך **לעשות** עם המספר. זה ה"וואו" של הפרויקט.

- תחזית היא מספר או מחלקה ("סיכון 0.9", "סגמנט 3"). זה מה שהמודל מוציא.
- המלצה היא פעולה אחת שנגזרת מהמספר ("להגביר כוחות", "לכו למקומות האלה").

> 🧭 מבחן ה"אז מה?": לכל תחזית, שאלו "אז מה?" עד שמגיעים לפעולה אנושית. "חיזינו עומס תנועה". אז מה? "המשתמש יודע מתי לצאת". אז מה? "מגיע בזמן בלי לחץ". עוצרים כשיש פעולה שאדם אמיתי עושה.

---

## שתי דרכים לייצר את ההמלצה

- מבוסס-חוקים: אם התחזית מעל סף, פעולה א'; אחרת פעולה ב'. דטרמיניסטי, שקוף, ובטוח — ברירת מחדל מצוינת ל-MVP.
- מנוסח-LLM: מחזירים את פלט המודל ל-LLM, והוא מנסח המלצה ברורה — מעוגנת במספר, לא ממציאה אותו.

> 🔒 הכלל המקודש חל גם על הפלט: ההמלצה חייבת להישען על תוצאת המודל. ה-LLM מסביר וממליץ על בסיס המספר האמיתי — לא ממציא מספר, ולא המלצה מנותקת ממנו.

---

## מתקדם: tool use מקורי (function calling)

בשכבת הקלט ה-LLM החזיר JSON ואתם קראתם ל-`predict` בעצמכם. בצורה המתקדמת רושמים את `predict` כ**כלי**, והמודל מחליט לבד מתי לקרוא לו. היתרון: כאן "המספר מהמודל" הוא מובנה — ה-LLM פיזית לא מחשב אותו, רק מבקש את הכלי ומנסח את מה שחזר. שלב ראשון (Turn 1) הוא הקלט; שלב שני (Turn 2) הוא ההמלצה המנוסחת.

```python
# Native tool use: register predict as a tool; the model decides when to call it.
import json, pandas as pd, streamlit as st
from groq import Groq
client = Groq(api_key=st.secrets["GROQ_API_KEY"])
MODEL = "llama-3.3-70b-versatile"            # supports tools on Groq

tools = [{"type": "function", "function": {
    "name": "predict_price",
    "description": "Returns the team's trained-model prediction.",
    "parameters": {"type": "object",
        "properties": {"area": {"type": "number"}, "rooms": {"type": "integer"}, "city": {"type": "string"}},
        "required": ["area", "rooms", "city"]}}}]

messages = [
    {"role": "system", "content": "When a number is needed, call predict_price. Never invent it."},
    {"role": "user",   "content": user_text}]

# Turn 1 — input layer: the model decides to call the tool
r1 = client.chat.completions.create(model=MODEL, messages=messages,
                                    tools=tools, tool_choice="auto", temperature=0)
msg = r1.choices[0].message
tc  = msg.tool_calls[0]                       # .function.arguments is a JSON STRING
args = json.loads(tc.function.arguments)

# YOUR model runs — the LLM never produces the number
result = pipe.predict(pd.DataFrame([args]))[0]

# Turn 2 — prescriptive layer: feed the result back, get a grounded recommendation
messages.append(msg)
messages.append({"role": "tool", "tool_call_id": tc.id,
                 "name": tc.function.name, "content": str(result)})
messages.append({"role": "system", "content": "Give ONE concrete action, grounded in the number. Do not invent a number."})
r2 = client.chat.completions.create(model=MODEL, messages=messages, temperature=0)
st.success(r2.choices[0].message.content)
```

> ▶️ הדגמה רצה (Groq + Llama): [`M4_tool_use_demo.py`](M4_tool_use_demo.py) — לולאה מלאה שבה ההמלצה נבנית על הסגמנט שהמודל החזיר, כך שהמודל **לא נשכח**. בקובץ יש `assert` שמוודא שפלט המודל מופיע בהמלצה. הרצה: `python M4_tool_use_demo.py`.

> 🟦 ל-MVP צורת ה-JSON מהחלק הראשון מספיקה לגמרי. ה-function calling הוא שדרוג — מתאים במיוחד לסוכן שיחה שזוכר את ההקשר.

---

## וזיכרון?

סוכן מלא = מוח (LLM) + כלים + **זיכרון**. את הזיכרון מוסיפים רק כשיש דו-שיח אמיתי עם היוזר ("זוכר שאמרת שאתה בחיפה"). לגלאי או למחשבון חד-פעמי זה לא נדרש, וכפייה שלו היא ריח רע. הוסיפו רק אם זה משרת את הסיפור.

---

## בדיקת מוכנות הסוכן

- [ ] היוזר יכול לכתוב בשפה חופשית, והסוכן מחלץ פרמטרים תקינים?
- [ ] הפרמטרים עוברים ל-`predict` של **המודל שלכם** (ולא ה-LLM ממציא מספר)?
- [ ] הסוכן לא עוצר במספר — הוא נותן **המלצת פעולה אחת** מעוגנת בו?
- [ ] יש `Fallback` כשהפלט לא תקין?
- [ ] המפתח ב-`st.secrets`, ולא בקוד ולא בגיט?
- [ ] האפליקציה עולה ל-deploy עם כתובת ציבורית?

> 🎯 הסוכן שאתם מבינים ובוטחים בו היום = מוצר מוכן למרתון. המספר מהמודל שלכם, השפה מה-LLM, והאחריות שהפלט כן — שלכם.
